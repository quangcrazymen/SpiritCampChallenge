// Requirements: With an array of integer,
// find k elements that appear most frequently
// Check test cases for examples
export const findTopNFrequentNumber = (inputArr, k) => {
  //code here

  return;
};

// Requirements: generate first k elements
// from fibonacci sequence
// Using recursion
export const fibonacciGenerator = (k) => {
  //code here

  return;
};
